# Required for voice calls (Vapi dashboard → API keys → Public key)
VITE_VAPI_PUBLIC_KEY=

# Optional: override backend URL in production. Local dev uses Vite proxy to http://localhost:3001.
VITE_API_BASE_URL=

# Optional: log sessions to Supabase (create table interview_sessions — see src/lib/sessionPersistence.ts)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
